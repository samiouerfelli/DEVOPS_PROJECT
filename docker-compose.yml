version: '3.8'

services:
  discovery-server:
    image: samixouerfelli/discovery-server:latest
    ports:
      - "8761:8761"
    environment:
      SPRING_PROFILES_ACTIVE: local
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
