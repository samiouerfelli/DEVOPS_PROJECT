apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: myapp
spec:
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 8080  # Changed to 8080 to match the example
      targetPort: 8080
  type: LoadBalancer  # Changed to LoadBalancer like in the example